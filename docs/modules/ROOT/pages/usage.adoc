= Guide d’utilisation
:page-description: Utiliser Adoc Link Checker pour vérifier les liens AsciiDoc.
:icons: font
:source-highlighter: pygments

== 🚀 Utilisation de base

=== Vérifier un fichier `.adoc`

[source,bash]
----
adocx check-links README.adoc --output report.json
----

=== Vérifier un dossier

[source,bash]
----
adocx check-links ./docs --output report.json
----

== ⚙️ Options disponibles

[cols="1,2,1"]
|===
| Option | Description | Obligatoire

| `<FILE_OR_DIR>` | Fichier `.adoc` ou dossier à analyser | Oui
| `--output` | Fichier JSON de sortie (**aucun défaut**) | Oui
| `--timeout` | Timeout HTTP (secondes) | Non
| `--max-workers` | Nombre de threads | Non
| `--delay` | Délai entre requêtes | Non
| `--blacklist` | Domaine à ignorer | Non
| `--exclude-from` | Fichier de liens à exclure | Non
| `-v / -vv` | Verbosité | Non
| `--quiet` | Mode silencieux | Non
|===

== 📁 Exemple complet

[source,bash]
----
adocx check-links ./docs \
  --output report.json \
  --max-workers 8 \
  --timeout 20 \
  --exclude-from exclude_urls.txt \
  -vv
----

== 📌 Notes importantes

- L’option `--output` est **obligatoire**
- Aucun fichier n’est généré par défaut
- L’absence de `--output` provoque une erreur CLI
- Le code retour est `0` même si des liens sont cassés  
  (option `--fail-on-broken` prévue)
