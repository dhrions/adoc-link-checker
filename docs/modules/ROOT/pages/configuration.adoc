= Configuration avancée
:page-description: Options avancées d’Adoc Link Checker.
:icons: font
:source-highlighter: pygments

== ⚙️ Options CLI

[cols="1,2,1"]
|===
| Option | Description | Défaut

| `--timeout` | Timeout HTTP (secondes) | 15
| `--max-workers` | Threads parallèles | 5
| `--delay` | Délai entre requêtes | 0.5
| `--blacklist` | Domaine à ignorer | -
| `--exclude-from` | Fichier d’exclusion | -
| `-v / -vv` | Verbosité | -
| `--quiet` | Logs erreurs uniquement | -
|===

== 📝 Fichier d’exclusion

Le fichier d’exclusion permet d’ignorer des URLs spécifiques.

[source,text]
----
# Commentaires autorisés
https://example.com/temp
https://dev.example.com
----

- Une URL par ligne
- Les lignes vides ou commentées (`#`) sont ignorées

== 🌐 Comportement HTTP

- Requête `HEAD` prioritaire
- Fallback automatique vers `GET`
- Redirections suivies
- User-Agent configuré par défaut

== 📌 Bonnes pratiques

- Limiter `--max-workers` pour éviter les bans
- Exclure les URLs dynamiques
- Utiliser `-vv` pour diagnostiquer les faux positifs
