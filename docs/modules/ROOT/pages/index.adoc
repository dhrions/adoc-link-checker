= Adoc Link Checker
:page-description: Outil CLI pour vérifier les liens brisés dans des fichiers AsciiDoc.
:icons: font
:source-highlighter: pygments

== 🔍 Présentation

Adoc Link Checker est un outil en ligne de commande permettant de **détecter automatiquement les liens HTTP/HTTPS brisés**
dans des fichiers AsciiDoc (`.adoc`).

Il est conçu pour être :

- fiable (HEAD + fallback GET),
- scriptable (CI/CD),
- explicite (aucune sortie implicite).

== 🚀 Fonctionnalités

- Vérification des liens HTTP/HTTPS
- Support des IDs YouTube (`video::ID[]`)
- Traitement parallèle
- Exclusion de liens ou domaines
- Rapport JSON explicite
- Utilisation sur **un fichier ou un dossier**

== 🧭 Commande principale

[source,bash]
----
adocx check-links <FILE_OR_DIR> --output report.json
----

- `<FILE_OR_DIR>` : fichier `.adoc` ou dossier
- `--output` : **obligatoire**, fichier JSON de sortie

== 📌 Exemple rapide

[source,bash]
----
# Vérifier un fichier unique
adocx check-links README.adoc --output report.json

# Vérifier un dossier
adocx check-links ./docs --output report.json
----

== 📊 Résultat

Le rapport JSON contient les liens brisés, regroupés par fichier :

[source,json]
----
{
  "docs/page.adoc": [
    ["https://example.com/broken", "URL not accessible"]
  ]
}
----

== 📖 Documentation

- xref:installation.adoc[Installation]
- xref:usage.adoc[Utilisation]
- xref:configuration.adoc[Configuration avancée]
